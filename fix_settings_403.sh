sed -i 's/val errorBody = response.body?.string() ?: ""/val errorBody = response.body?.string() ?: ""\n                                                if (response.code == 403) {\n                                                    withContext(Dispatchers.Main) {\n                                                        android.widget.Toast.makeText(context, if (isArabic) "مفتاح API محظور أو غير صالح (خطأ 403). يرجى تغييره." else "API key blocked or invalid (403). Please change it.", android.widget.Toast.LENGTH_LONG).show()\n                                                    }\n                                                }/g' app/src/main/java/com/example/ui/settings/SettingsScreen.kt
